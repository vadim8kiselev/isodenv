#! /bin/sh
case "$1" in
start)
  bash /isodenv/scripts/start_gsa.sh
  ;;
status)
  #bash /isodenv/scripts/...
  echo "command does not work"
  ;;
kill)
  sudo bash /isodenv/scripts/gscheck.sh
  ;;
restart)
  sudo bash /isodenv/scripts/gscheck.sh
  bash /isodenv/scripts/start_gsa.sh
  ;;
start-ui)
  bash /isodenv/scripts/gs-webui-start.sh
  ;;
kill-ui)
  sudo bash /isodenv/scripts/gs-webui-stop.sh
  ;;
restart-ui)
  sudo bash /isodenv/scripts/gs-webui-stop.sh
  bash /isodenv/scripts/gs-webui-start.sh
  ;;
health-check)
  bash /isodenv/scripts/health_check.sh
  ;;
deploy-app)
  bash /isodenv/scripts/deploy_script.sh
  ;;
*)
  echo "Usage: service gigaspaces {start|stop|restart|status|start-ui|kill-ui|restart-ui|health-check|deploy-app}" > &2
  exit 1
  ;;
esac
